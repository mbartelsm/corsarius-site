---
interface Props {
    date: string;
    format: string;
}

const { date, format } = Astro.props;

import dayjs from "dayjs";
import advancedFormat from "dayjs/plugin/advancedFormat";
dayjs.extend(advancedFormat);

const pub_date = dayjs(date).format(format);

---

<span>{pub_date}</span>
